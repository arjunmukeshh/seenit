node_modules/
dist/
*.log

# The analysis ledger and its cache live inside the *target* repo's .git/
# directory (.git/seenit/ledger.git), so they are never part of a working tree
# and need no ignore rule. Local scratch output does:
calibration/data/
calibration/.venv/
*.tgz
