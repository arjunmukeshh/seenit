node_modules/
dist/
*.log

# The analysis ledger lives inside the *target* repo's .git/ directory
# (.git/gitcodebase/ledger.git), so it is never part of a working tree and
# needs no ignore rule. Local scratch output does:
.gitcodebase-cache/
calibration/data/
calibration/.venv/
